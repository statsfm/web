import { Container } from '@/components/Container';
import type { NextPage } from 'next';

const friendsPage: NextPage = () => {
  return <Container>Friends</Container>;
};

export default friendsPage;
